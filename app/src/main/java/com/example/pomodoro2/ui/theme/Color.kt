package com.example.pomodoro2.ui.theme

import androidx.compose.ui.graphics.Color

// Màu nhấn chủ đạo (Tím & Cam hồng)
val MauNhanTym = Color(0xFFD384F2)
val MauNutCam = Color(0xFFFF6D7F)

// Màu cho Chế độ Tối (Dark Mode)
val NenToi = Color(0xFF1A1A2E)
val ChuTrang = Color(0xFFE5E3FF)
val MauKinhToi = Color(0xFFD384F2).copy(alpha = 0.1f)
val VongTronToi = Color(0xFF1A1A2E).copy(alpha = 0.5f)

// Màu cho Chế độ Sáng (Light Mode)
val NenSang = Color(0xFFF8F9FF)
val ChuToi = Color(0xFF1A1A2E)
val MauKinhSang = Color(0xFFD384F2).copy(alpha = 0.05f)
val VongTronSang = Color(0xFFE0E0E0).copy(alpha = 0.5f)